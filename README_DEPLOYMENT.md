# 🚀 Полный гайд развертывания сайта Jumarf 24/7

## 📋 Описание
Современный link-in-bio сайт с анимированным дизайном, видеоплеером и социальными ссылками. Работает на React + Vite.

## 🎯 Цель
Развернуть сайт для работы 24/7 без VPN с доступом из РФ на доменах `jumarf.ru` и `jumarf.fun`.

---

## 🏆 Рекомендуемый вариант: GitHub Pages + Custom Domains

### ✅ Преимущества
- **Полностью бесплатно 24/7**
- **Стабильная работа в РФ**
- **Надежный хостинг от GitHub**
- **Автоматический HTTPS**
- **Поддержка custom domains**

---

## 📝 Пошаговая инструкция

### Шаг 1: Подготовка репозитория

```bash
# Инициализация git (если еще не сделано)
git init
git add .
git commit -m "Initial commit - React link-in-bio site"

# Добавление remote (замените на ваш репозиторий)
git remote add origin https://github.com/yourusername/ezbio.git
git branch -M main
git push -u origin main
```

### Шаг 2: Настройка GitHub Pages

1. **Откройте ваш репозиторий на GitHub**
2. **Settings → Pages**
3. **Source**: `Deploy from a branch`
4. **Branch**: `main` / `(root)`
5. **Нажмите Save**

### Шаг 3: Настройка Custom Domains

1. **В Settings → Pages** добавьте домены:
   - `jumarf.ru`
   - `jumarf.fun`

2. **GitHub покажет DNS записи**, которые нужно добавить:
   ```
   Тип: A
   Имя: @
   Значение: 185.199.108.153
   Значение: 185.199.109.153
   Значение: 185.199.110.153
   Значение: 185.199.111.153
   
   Тип: CNAME
   Имя: www
   Значение: yourusername.github.io
   ```

### Шаг 4: Настройка DNS у регистратора доменов

**Для jumarf.ru и jumarf.fun добавьте:**

```
# A-записи (основной домен)
Тип: A
Имя: @
TTL: 3600
Значения:
- 185.199.108.153
- 185.199.109.153
- 185.199.110.153
- 185.199.111.153

# CNAME для www
Тип: CNAME
Имя: www
TTL: 3600
Значение: yourusername.github.io

# CNAME для подтверждения (если GitHub запросит)
Тип: CNAME
Имя: _github-challenge-yourusername.jumarf.ru
Значение: yourusername.github.io
```

### Шаг 5: Проверка развертывания

1. **Дождитесь обновления DNS** (5-30 минут)
2. **Проверьте работу:**
   - `https://jumarf.ru`
   - `https://jumarf.fun`
   - `https://yourusername.github.io/ezbio`

---

## 🔧 Файлы конфигурации

### `vite.config.js`
```javascript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  base: '/ezbio/', // Имя вашего репозитория
  build: {
    outDir: 'dist',
    assetsDir: 'assets'
  }
})
```

### `CNAME` (в корне репозитория)
```
jumarf.ru
```

### `.github/workflows/deploy.yml`
```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '22'
        cache: 'npm'
        
    - name: Install pnpm
      uses: pnpm/action-setup@v3
      with:
        version: 8
        
    - name: Install dependencies
      run: pnpm install
      
    - name: Build
      run: pnpm build
      
    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      if: github.ref == 'refs/heads/main'
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist
```

---

## 🚀 Альтернативные варианты

### Вариант 2: Surge.sh (быстрый старт)

```bash
# Развертывание
pnpm build
npx surge dist jumarf.ru
npx surge dist jumarf.fun

# Обновление
pnpm build
npx surge dist jumarf.ru --domain jumarf.ru
```

**Плюсы:**
- Мгновенное развертывание
- Простота настройки

**Минусы:**
- Требует переобновления каждые 30 дней для бесплатного тарифа

### Вариант 3: Vercel (с ограничениями)

```bash
# Развертывание
npm i -g vercel
vercel --prod
```

**Плюсы:**
- Отличный CDN
- Простая настройка

**Минусы:**
- Ограничение на бездействие бесплатных сайтов

---

## 🛠️ Обновление сайта

### Автоматические обновления (GitHub Pages)
```bash
# Любые изменения в main ветке автоматически развертываются
git add .
git commit -m "Update site content"
git push origin main
```

### Ручное обновление (Surge.sh)
```bash
pnpm build
npx surge dist jumarf.ru
npx surge dist jumarf.fun
```

---

## 🔍 Проверка работоспособности

### Тестирование доступности
```bash
# Проверка доменов
ping jumarf.ru
ping jumarf.fun

# Проверка HTTPS
curl -I https://jumarf.ru
curl -I https://jumarf.fun

# Проверка из разных регионов
# Используйте онлайн сервисы: ping.pe, gtmetrix.com
```

### Мониторинг
- **Uptime Robot** (бесплатный мониторинг)
- **StatusCake** (проверка доступности)
- **Pingdom** (мониторинг производительности)

---

## 📱 Оптимизация для мобильных устройств

Сайт уже оптимизирован:
- Адаптивный дизайн
- Оптимизированные изображения
- Lazy loading для видео
- Кэширование статических ресурсов

---

## 🔒 Безопасность

### HTTPS
- Автоматически настраивается GitHub Pages
- Бесплатный SSL-сертификат
- HSTS включен по умолчанию

### Защита контента
```javascript
// В _headers файле
/*
  X-Frame-Options: DENY
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
```

---

## 🚨 Возможные проблемы и решения

### DNS не обновляется
```
Проблема: Домен не открывается после настройки
Решение: 
1. Подождите до 48 часов для обновления DNS
2. Проверьте записи через nslookup
3. Очистите кэш DNS: ipconfig /flushdns
```

### Сайт не работает в РФ
```
Проблема: GitHub Pages блокируется
Решение:
1. Используйте Surge.sh как резервный вариант
2. Настройте Cloudflare как прокси (если доступен)
3. Рассмотрите VPS с российским IP
```

### Ошибка сборки
```
Проблема: Build failed в GitHub Actions
Решение:
1. Проверьте vite.config.js (base path)
2. Убедитесь что все зависимости в package.json
3. Проверьте синтаксис в CNAME файле
```

---

## 📊 Сравнение платформ

| Платформа | 24/7 | РФ | Бесплатно | Custom domains | CDN |
|-----------|------|----|-----------|----------------|-----|
| **GitHub Pages** | ✅ | ✅ | ✅ | ✅ | ✅ |
| Surge.sh | ✅ | ✅ | ⚠️ | ✅ | ✅ |
| Vercel | ⚠️ | ✅ | ⚠️ | ✅ | ✅ |
| Netlify | ✅ | ❌ | ✅ | ✅ | ✅ |

---

## 🎯 Рекомендация

**Используйте GitHub Pages** как основной вариант:
- Максимальная стабильность
- Полностью бесплатно
- Отличная работа в РФ
- Автоматические обновления

**Surge.sh** как резервный вариант для быстрого тестирования.

---

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте [GitHub Pages documentation](https://docs.github.com/en/pages)
2. Используйте [GitHub community forums](https://github.community/)
3. Проверьте статус GitHub: [status.github.com](https://www.githubstatus.com/)

---

**✅ После выполнения этой инструкции ваш сайт будет работать 24/7 на jumarf.ru и jumarf.fun без VPN для пользователей из РФ.**
